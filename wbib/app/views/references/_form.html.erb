<%= form_for(@reference) do |f| %>
	<% if @reference.errors.any? %>
		<div id="error_explaination">
			<h2><%= pluralize(@reference.errors.count, "error") %> prohibited this reference from being saved:</h2>

			<ul>
				<% @reference.errors.full_message.each do |message| %>
					<li><%= message %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="field">
		<%= f.label :url %><br>
		<%= f.text_field :url %>
	</div>
	
	<div class="field">
		<%= f.label :topic %><br>
		<%= f.text_field :topic %>
	</div>

	<div class="field">
		<%= f.label :annotation %><br>
		<%= f.text_field :annotation %>
	</div>

	<div class="field">
		<%= f.label :timedate %><br>
		<%= f.date_field :timedate %>
	</div>

	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>
